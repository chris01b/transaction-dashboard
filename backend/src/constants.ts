// Pagination constants
export const DEFAULT_PAGE_SIZE = 10;
export const MAX_PAGE_SIZE = 50;
// Maximum number of transactions to process to prevent excessive memory usage
export const MAX_TRANSACTIONS = 10000;
